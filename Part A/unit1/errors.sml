(* 调试例子 *)

val x = 34;

val y = x + 1;

val z = if y > 34 then 34 else x - 4;

val q = if y > 0 then 0 else 1;

		      val a = ~5
		      val w = 0;
		      val fx = 34;
		      val v = x div 1
		      
		      
